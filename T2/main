clc; clear all;

% Inicialização de Parâmetros
t = 1; n = 10^4;

%% Kalman

while (t<n)
    t = t+1;
    
    % Predição
    mib(t) = A(t)*mi(t-1) + B(t)*u(t);
    sigmab(t)
    
    
    % Correção
    
end
